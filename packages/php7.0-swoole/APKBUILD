# Maintainer: Peter Kokot <peterkokot@gmail.com>

pkgver=4.0.2
pkgrel=0
_phpver=7.0
_php=php$_phpver
_pkgreal=swoole
pkgname=$_php-$_pkgreal
pkgdesc="PHP $_phpver extension - Event-driven asynchronous and concurrent networking engine with high performance"
url="http://pecl.php.net/package/$_pkgreal"
arch="all"
license="Apache2.0"
depends="$_php-common"
makedepends="$_php-dev coreutils linux-headers"
source="http://pecl.php.net/get/$_pkgreal-$pkgver.tgz"
builddir="$srcdir/$_pkgreal-$pkgver"
options="!check"

prepare() {
  default_prepare
}

build() {
  local jobs=$(($(nproc)+1))

  cd "$builddir"

  phpize

  EXTENSION_DIR=/usr/lib/php/$_phpver/modules \
  CFLAGS="-fstack-protector-strong -fpic -fpie -O2" \
  CPPFLAGS="$CFLAGS" \
  LDFLAGS="-Wl,-O1 -Wl,--hash-style=both -pie" \
  ./configure \
    --prefix=/usr \
    --with-php-config=/usr/bin/php-config

  make -j$jobs
}

check() {
  cd "$builddir"

  local allow_fail='yes'

  NO_INTERACTION=1 REPORT_EXIT_STATUS=1 \
    SKIP_SLOW_TESTS=1 SKIP_ONLINE_TESTS=1 TEST_TIMEOUT=10 \
    TZ= LANG= LC_ALL= \
    make test || [ "$allow_fail" = yes ]
}

package() {
  mkdir -p "$pkgdir"
  install -d $pkgdir/
  make -C "$builddir" INSTALL_ROOT="$pkgdir" install
  install -d "$pkgdir"/etc/php/$_phpver/conf.d
  echo "extension=$_pkgreal.so" > "$pkgdir"/etc/php/$_phpver/conf.d/$_pkgreal.ini
}
sha512sums="16b9bf3a6e72764875bf1447802453c32aba05e205811d169eba605430cc3fb59a92a9b989461bc3d9bba203873ce09eb0249b9ba732c133a51873ddb2223a23  swoole-4.0.2.tgz"
